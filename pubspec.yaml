name: adsparkle_flutter
description: Flutter client SDK for the AdSparkle affiliate attribution tracking platform. Pure Dart, no platform channels.
version: 0.1.4
repository: https://github.com/Adsparkle/adsparkle-flutter

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  shared_preferences: ^2.0.0

dev_dependencies:
  flutter_lints: ^3.0.0
